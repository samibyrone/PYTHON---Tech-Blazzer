
value = 3.14159
 
radius = float(input(" Enter the circle of a radius: "))

area = value * radius * radius
diameter = radius * radius
circumference = value * value * radius

print("\n Circle of a Area = %.2f" %area)
print("\n Circle of a Diameter = %.2f" %diameter)
print("\n Circle of a Circumference = %.2f" %circumference)